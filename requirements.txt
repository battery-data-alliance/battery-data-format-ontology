# Core dependencies for ontology parsing and linked data processing
rdflib
PyYAML

# OWL reasoning and EMMO bindings — required for local development
# and the ontology_toolkit.py scripts, but not for CI validation.
# Install separately when needed: pip install EMMOntoPy owlrl
# EMMOntoPy also requires a Java runtime for the OWL reasoner.
# EMMOntoPy>=0.4.0,<1
# owlrl

# Optional development tools
# jupyter
# ipykernel
