name: docs

on:
  push:
    branches: [main]
    tags: ['[0-9]*.[0-9]*.[0-9]*']
  pull_request:
    branches: ['*']

# One docs build per ref at a time. A newer push to main cancels an
# in-progress build so the most recent commit is always deployed.
concurrency:
  group: docs-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    name: Build and deploy documentation
    runs-on: ubuntu-latest

    permissions:
      contents: write   # required by peaceiris/actions-gh-pages

    steps:
      - name: Checkout (full history for tag enumeration)
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Cache pip
        uses: actions/cache@v4
        with:
          path: ~/.cache/pip
          key: pip-docs-${{ runner.os }}-${{ hashFiles('pyproject.toml') }}
          restore-keys: pip-docs-${{ runner.os }}-

      - name: Install Python dependencies
        run: |
          python -m pip install --upgrade pip
          pip install ".[docs]"

      - name: Install system dependencies (pandoc)
        run: sudo apt-get update -y && sudo apt-get install -y --no-install-recommends pandoc

      - name: Set build environment variables
        id: set-env
        run: |
          python .github/scripts/ontology_toolkit.py --print-env >> $GITHUB_ENV
          echo "doc_date=$(date +%Y-%m-%d)" >> $GITHUB_OUTPUT

      - name: Generate RST documentation from TTL
        env:
          ONLY_LOCAL_IMPORTS: "1"
        run: python .github/scripts/ontology_toolkit.py --generate-rst

      - name: Build HTML with Sphinx (warnings as errors)
        run: sphinx-build -b html -W --keep-going docs/ docs/_build/html/

      - name: Fetch FOOPS score and generate badge SVG
        # Non-fatal: a transient outage of foops.linkeddata.es must not block deployment.
        # The badge SVG is written to docs/assets/ and picked up by the assets copy below.
        continue-on-error: true
        run: python .github/scripts/foops_toolkit.py --generate-badge

      - name: Prepare deploy directory structure
        run: mkdir -p docs/_build/html/{context,schema/vendors,vendors,assets,versions}

      - name: Copy static assets, context, schema, and vendor artifacts to build output
        run: |
          cp -r docs/assets/. docs/_build/html/assets/ 2>/dev/null || true
          cp context/context.json docs/_build/html/context/
          cp schema/schema.json   docs/_build/html/schema/
          cp -r schema/vendors/.  docs/_build/html/schema/vendors/
          cp -r vendors/.         docs/_build/html/vendors/

      - name: Apply FOOPS recommendations to TTL files
        run: |
          for FILE in $(python .github/scripts/ontology_toolkit.py --print-ttl-files); do
            if [ -f "$FILE" ]; then
              OUTPUT="${FILE%.ttl}-foops.ttl"
              python .github/scripts/foops_toolkit.py --apply-to-file "$FILE" "$OUTPUT"
              cp "$OUTPUT" "docs/_build/html/$(basename "$FILE")"
            fi
          done

      - name: Copy versioned artefacts for each git tag
        run: |
          while IFS= read -r TAG; do
            [ -z "$TAG" ] && continue
            echo "Processing tag: $TAG"
            mkdir -p "docs/_build/html/versions/$TAG/context"
            mkdir -p "docs/_build/html/versions/$TAG/schema"
            git show "$TAG:battery-data-format.ttl"    > "docs/_build/html/versions/$TAG/battery-data-format.ttl"
            git show "$TAG:context/context.json"        > "docs/_build/html/versions/$TAG/context/context.json"
            git show "$TAG:schema/schema.json"          > "docs/_build/html/versions/$TAG/schema/schema.json"
          done < <(git tag)

      - name: Verify the current release has a published version snapshot
        # Runs on main/tag pushes (deploy-bound builds), not PRs: a release PR
        # legitimately bumps owl:versionInfo before its tag is pushed, so a PR-time
        # check would fail spuriously. On main the tag must already exist.
        if: github.event_name == 'push'
        run: |
          V=$(grep -oP 'owl:versionInfo\s+"\K[0-9.]+' battery-data-format.ttl | head -n1)
          if [ -z "$V" ]; then
            echo "::error::Could not parse owl:versionInfo from battery-data-format.ttl"
            exit 1
          fi
          echo "Ontology declares version $V"
          if [ ! -f "docs/_build/html/versions/$V/battery-data-format.ttl" ]; then
            echo "::error::versions/$V/battery-data-format.ttl was not built: the ontology declares version $V but no matching git tag exists, so owl:versionIRI .../$V/... will 404. Push the tag with 'git tag $V && git push origin $V' to publish the versioned snapshot."
            exit 1
          fi
          echo "Verified versions/$V/battery-data-format.ttl is present"

      - name: Upload documentation artifact
        uses: actions/upload-artifact@v4
        with:
          name: "bdf-ontology-docs-${{ steps.set-env.outputs.doc_date }}"
          path: docs/_build/html/

      - name: Deploy to GitHub Pages
        if: github.event_name == 'push' && (github.ref == 'refs/heads/main' || startsWith(github.ref, 'refs/tags/'))
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: docs/_build/html/
